interface:
  display_name: "Agent Workspaces"
  short_description: "Coordinate isolated agent worktrees"
  default_prompt: "Use $orchestrate-agent-workspaces to plan isolated parallel tasks and integrate their commits safely."
